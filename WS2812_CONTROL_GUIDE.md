# WS2812 多通道控制系统

## 概述
ESP32S3支持4通道WS2812灯带控制，使用GPIO 18、19、20、21。

## 硬件配置
- 通道0: GPIO 18
- 通道1: GPIO 19  
- 通道2: GPIO 20
- 通道3: GPIO 21
- 每通道支持160个LED（可在ws2812_control.h中修改）

## 控制协议

### JSON命令格式（唯一支持的格式）

#### 1. 控制单个通道
```json
{
  "channel": 0,
  "mode": 1,
  "color": {"r": 255, "g": 0, "b": 0},
  "brightness": 128,
  "speed": 100
}
```

#### 2. 广播控制（所有通道）
```json
{
  "channel": 255,
  "mode": 2,
  "brightness": 200
}
```

#### 3. 启用/禁用通道
```json
{
  "channel": 1,
  "enabled": false
}
```

#### 4. 查询状态
```json
{
  "action": "status"
}
```

#### 5. 设置自动循环持续时间
```json
{
  "channel": 0,
  "cycle_duration": 5000
}
```

## 模式说明

| 模式值 | 模式名称 | 说明 | 速度控制 | 颜色控制 |
|--------|----------|------|----------|----------|
| 0 | OFF | 关闭 | - | - |
| 1 | STATIC | 静态颜色 | - | ✅ |
| 2 | RAINBOW | 彩虹效果 | ✅ | - |
| 3 | BREATHING | 呼吸灯效果 | ✅ | ✅ |
| 4 | RUNNING | 跑马灯效果 | ✅ | ✅ |
| 5 | FLASH | 闪烁效果 | ✅ | ✅ |
| 6 | WAVE | 波浪效果 | ✅ | ✅ |
| 7 | AUTO_CYCLE | 自动循环模式 | ✅ | 自动变化 |

### 详细效果说明

#### 呼吸灯效果 (BREATHING)
- **速度控制**: `speed` 值越小呼吸越快，建议范围 50-2000ms
- **颜色控制**: 使用指定颜色进行呼吸，亮度自动变化
- **效果**: 灯光在指定颜色基础上做亮度的正弦波变化

#### 跑马灯效果 (RUNNING)  
- **速度控制**: `speed` 控制移动速度，建议范围 50-1000ms
- **颜色控制**: 使用指定颜色，带有5个LED的渐变拖尾
- **效果**: 带拖尾的光点在灯带上移动

#### 闪烁效果 (FLASH)
- **速度控制**: `speed` 控制闪烁周期，完整周期为 speed×2
- **颜色控制**: 使用指定颜色闪烁
- **效果**: 全亮指定颜色 → 全灭 → 循环

## 串口命令

### JSON命令发送方式
支持以下两种发送方式，**无需回车符**：

1. **直接发送完整JSON**（推荐）: 
   ```json
   {"channel": 0, "mode": 1}
   ```
   
2. **前缀格式**（需要回车）: 
   ```
   JSON:{"channel": 0, "mode": 1}
   ```

### 智能JSON检测
- 系统会自动检测以 `{` 开头的JSON命令
- 当检测到完整的JSON对象（大括号匹配）时立即处理
- 支持嵌套JSON结构（如color对象）
- **不需要手动添加回车符或换行符**

### 基础命令（需要回车）
- `BATTERY` - 查询电池状态
- `WS2812:TEST` - 测试所有通道
- `HELP` - 显示帮助信息

## 参数范围

- **channel**: 0-3（单通道）或 255（广播）
- **mode**: 0-7
- **color.r/g/b**: 0-255
- **brightness**: 0-255
- **speed**: 1-10000 毫秒
- **cycle_duration**: 1000-60000 毫秒

## 使用示例

### 1. 设置通道0为红色静态灯
```json
{"channel": 0, "mode": 1, "color": {"r": 255, "g": 0, "b": 0}, "brightness": 100}
```

### 2. 所有通道彩虹模式
```json
{"channel": 255, "mode": 2, "brightness": 150}
```

### 3. 通道1红色呼吸灯（快速）
```json
{"channel": 1, "mode": 3, "color": {"r": 255, "g": 0, "b": 0}, "brightness": 150, "speed": 100}
```

### 4. 通道2蓝色呼吸灯（慢速）
```json
{"channel": 2, "mode": 3, "color": {"r": 0, "g": 0, "b": 255}, "brightness": 150, "speed": 1000}
```

### 5. 通道0绿色跑马灯（快速移动）
```json
{"channel": 0, "mode": 4, "color": {"r": 0, "g": 255, "b": 0}, "brightness": 200, "speed": 80}
```

### 6. 通道3紫色跑马灯（慢速移动）
```json
{"channel": 3, "mode": 4, "color": {"r": 128, "g": 0, "b": 255}, "brightness": 180, "speed": 500}
```

### 7. 所有通道白色闪烁（快速）
```json
{"channel": 255, "mode": 5, "color": {"r": 255, "g": 255, "b": 255}, "brightness": 200, "speed": 200}
```

### 8. 通道1黄色闪烁（慢速）
```json
{"channel": 1, "mode": 5, "color": {"r": 255, "g": 255, "b": 0}, "brightness": 150, "speed": 800}
```

### 9. 禁用通道2
```json
{"channel": 2, "enabled": false}
```

### 10. 查看所有通道状态
```json
{"action": "status"}
```

### 11. 设置自动循环模式，每个效果持续3秒
```json
{"channel": 255, "mode": 7, "cycle_duration": 3000}
```

## 调试命令

- `WS2812:TEST` - 依次点亮每个通道2秒，用不同颜色区分：
  - 通道0: 红色
  - 通道1: 绿色
  - 通道2: 蓝色
  - 通道3: 黄色

## 响应格式

成功: `OK`
失败: `ERROR`
测试完成: `TEST_COMPLETED`
测试失败: `TEST_FAILED`
帮助显示: `HELP_DISPLAYED`

## 注意事项

1. 所有命令必须使用JSON格式，不再支持旧版UART命令
2. 广播ID（255）可以同时控制所有通道
3. 通道初始化失败会自动禁用该通道
4. 初始化时会进行2秒的通道测试
5. 建议使用较低的亮度值以节省功耗
