# WS2812 LED控制说明文档

本项目为ESP32-S3添加了WS2812 LED灯条控制功能，支持通过串口控制多种灯光效果。

## 硬件连接

- **WS2812数据引脚**: GPIO 19
- **LED数量**: 30个（可在ws2812_control.h中修改WS2812_LED_COUNT）
- **串口通信**: UART1 (TX: GPIO17, RX: GPIO18, 波特率: 115200)

## 串口命令格式

所有WS2812相关命令需要以 `WS2812:` 前缀开始，格式如下：

### 基本命令

1. **设置模式**
   ```
   WS2812:MODE:0    # 关闭所有LED
   WS2812:MODE:1    # 静态颜色模式
   WS2812:MODE:2    # 彩虹效果
   WS2812:MODE:3    # 呼吸灯效果
   WS2812:MODE:4    # 跑马灯效果
   WS2812:MODE:5    # 闪烁效果
   WS2812:MODE:6    # 波浪效果
   WS2812:MODE:7    # 自动循环模式 (默认上电模式)
   ```

2. **设置颜色** (RGB值，0-255)
   ```
   WS2812:COLOR:255,0,0      # 红色
   WS2812:COLOR:0,255,0      # 绿色
   WS2812:COLOR:0,0,255      # 蓝色
   WS2812:COLOR:255,255,255  # 白色
   WS2812:COLOR:255,165,0    # 橙色
   ```

3. **设置亮度** (0-255)
   ```
   WS2812:BRIGHTNESS:255     # 最亮
   WS2812:BRIGHTNESS:128     # 中等亮度
   WS2812:BRIGHTNESS:50      # 低亮度
   ```

4. **设置速度** (10-10000毫秒)
   ```
   WS2812:SPEED:100          # 快速 (100ms)
   WS2812:SPEED:500          # 中速 (500ms)
   WS2812:SPEED:1000         # 慢速 (1000ms)
   ```

5. **设置自动循环持续时间** (1000-60000毫秒)
   ```
   WS2812:CYCLE_DURATION:8000    # 每个效果持续8秒 (默认)
   WS2812:CYCLE_DURATION:5000    # 每个效果持续5秒
   WS2812:CYCLE_DURATION:12000   # 每个效果持续12秒
   ```

6. **查询状态**
   ```
   WS2812:STATUS             # 显示当前设置
   ```

7. **帮助信息**
   ```
   WS2812:HELP               # 显示所有可用命令
   ```

### 其他命令

- `BATTERY` - 查询电池状态

## 灯光效果说明

### MODE:0 - 关闭
- 所有LED关闭，无光效

### MODE:1 - 静态颜色
- 所有LED显示相同的静态颜色
- 颜色可通过COLOR命令设置
- 亮度可通过BRIGHTNESS命令调节

### MODE:2 - 彩虹效果  
- LED条显示流动的彩虹色彩
- 速度可通过SPEED命令调节
- 亮度可通过BRIGHTNESS命令调节

### MODE:3 - 呼吸灯效果
- LED按设定颜色进行呼吸式明暗变化
- 颜色可通过COLOR命令设置
- 呼吸速度可通过SPEED命令调节

### MODE:4 - 跑马灯效果
- 3个LED组成的亮点在LED条上循环移动
- 颜色可通过COLOR命令设置
- 移动速度可通过SPEED命令调节

### MODE:5 - 闪烁效果
- 所有LED同时闪烁
- 颜色可通过COLOR命令设置  
- 闪烁速度可通过SPEED命令调节

### MODE:7 - 自动循环模式 (默认上电模式)
- 自动在彩虹、呼吸灯、跑马灯、波浪、闪烁效果之间循环
- 每个效果默认持续8秒，可通过CYCLE_DURATION命令调整
- 每种效果使用不同的预设颜色
- 循环顺序：彩虹→呼吸灯(橙色)→跑马灯(青绿色)→波浪(紫色)→闪烁(红色)→循环重复

## 使用示例

### 串口工具连接设置
- 波特率: 115200
- 数据位: 8
- 停止位: 1
- 校验位: None
- 流控: None

### 常用命令组合

1. **启动默认自动循环效果** (上电默认)
   ```
   WS2812:MODE:7             # 自动循环模式
   WS2812:BRIGHTNESS:180     # 设置亮度
   WS2812:CYCLE_DURATION:8000 # 每个效果8秒
   ```

2. **设置红色静态灯光**
   ```
   WS2812:MODE:1
   WS2812:COLOR:255,0,0
   WS2812:BRIGHTNESS:128
   ```

3. **设置快速彩虹效果**
   ```
   WS2812:MODE:2
   WS2812:SPEED:50
   WS2812:BRIGHTNESS:200
   ```

4. **设置蓝色呼吸灯**
   ```
   WS2812:MODE:3
   WS2812:COLOR:0,0,255
   WS2812:SPEED:800
   WS2812:BRIGHTNESS:150
   ```

5. **设置橙色跑马灯**
   ```
   WS2812:MODE:4
   WS2812:COLOR:255,165,0
   WS2812:SPEED:200
   WS2812:BRIGHTNESS:180
   ```

6. **设置快速自动循环**
   ```
   WS2812:MODE:7
   WS2812:CYCLE_DURATION:5000  # 每个效果5秒
   WS2812:BRIGHTNESS:200
   ```

7. **关闭所有灯光**
   ```
   WS2812:MODE:0
   ```

## 响应消息

- `OK` - 命令执行成功
- `ERROR` - 命令执行失败
- `UNKNOWN_COMMAND` - 未知命令

## 注意事项

1. **电源供电**: WS2812 LED条需要充足的5V电源供电，30个LED在全亮状态下可能需要1.8A电流
2. **信号线连接**: 确保WS2812的数据线正确连接到ESP32-S3的GPIO19
3. **接地共用**: ESP32-S3和WS2812的电源地必须共地
4. **命令格式**: 严格按照格式发送命令，注意大小写和标点符号
5. **参数范围**: RGB值为0-255，亮度为0-255，速度为10-10000ms

## 故障排除

1. **LED不亮**: 检查电源连接和GPIO19连接
2. **颜色异常**: 检查命令格式是否正确
3. **无串口响应**: 检查串口连接和波特率设置
4. **部分LED不工作**: 检查LED条连接和电源供应能力

## 扩展功能

可以通过修改 `ws2812_control.h` 中的以下参数来适配不同配置：

- `WS2812_LED_COUNT`: LED数量 (默认30个)
- `WS2812_GPIO_PIN`: 数据引脚 (默认GPIO19)
- `WS2812_RMT_CHANNEL`: RMT通道 (默认0)

添加新的灯光效果可以在 `ws2812_control.c` 的 `ws2812_task` 函数中扩展新的case分支。
